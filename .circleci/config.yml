version: 2.0

jobs:
  build:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - checkout
      - run:
          name: "Testing"
          command: echo "Hi"
      - run:
          name: "Posso instalar o nodejs?"
          command: |
            apt install -y nodejs
            npm --version

