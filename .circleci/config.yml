version: 2.0

jobs:
  build:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - checkout
      - run:
          name: "Install Dependencies"
          command: npm install
      - run:
          name: "Execute"
          command: npm run report
      - store_artifacts:
          path: ./newman/
      - store_test_results:
          path: ./newman
